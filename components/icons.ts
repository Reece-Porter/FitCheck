import { Category } from "@/lib/types";

/* Simple monoline glyphs used as image placeholders per category. */
export const CAT_ICON: Record<Category, string> = {
  headwear: "⌒",
  outerwear: "❖",
  top: "▽",
  bottom: "‖",
  footwear: "◇",
  accessory: "○"
};
